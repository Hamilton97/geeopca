# Google Earth Engine Ordinaray Principal Component Analysis (PCA) Tools

## Build the Project
Install the required packages by running the following command in the terminal:
```bash
pip install -r requirements.txt
```



